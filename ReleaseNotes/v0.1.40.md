更新内容

- 修复 Pro 权益读取时偶发误选 `GPT-5.3-Codex-Spark` 专用额度的问题：macOS 与 Windows 现在会过滤 Codex-Spark / GPT-5.3 Spark 这类模型专用额度，只使用通用 Pro 使用限额作为可用额度来源。
- 增强额度字段兼容：除 `limit_id` / `limit_name` 外，同时识别 `id`、`slug`、`name`、`title`、`displayName` 等字段，避免接口字段变化导致专用额度漏过滤。
- 补充 macOS 与 Windows 额度解析回归测试，覆盖“通用额度剩余 83%，Codex-Spark 专用额度剩余 100% 时，导航栏应显示 83%”的场景。

安装说明

- Windows：下载 CodexPulse-Windows-Setup-0.1.40-x64.exe。
- macOS：下载 CodexPulse-macOS-0.1.40-universal.dmg。
